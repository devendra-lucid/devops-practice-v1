pipeline {
    agent any  // Use any available Jenkins agent
    stages {
        stage('Hello World') { // Define a stage
            steps {
                echo 'Hello, World!' // Print message to console
            }
        }
    }
}
